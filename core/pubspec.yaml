name: core
description: A new Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^6.0.1
  equatable: ^1.2.3
  retrofit: any
  json_serializable: ^3.3.0

  # This is our modular
  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: any
  build_runner: any

flutter: